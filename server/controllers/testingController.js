// server/controllers/indexController.js

// Sample controller
const testingController = (req, res) => {
  res.json({ message: "Welcome to the Pinterest Clone API! - testing" });
};

module.exports = {
  testingController,
};

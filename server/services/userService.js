// server/services/userService.js

// Sample service
const getUserById = (userId) => {
  // Replace this with actual logic to fetch user from the database
  return { id: userId, username: "sampleUser" };
};

module.exports = {
  getUserById,
};
